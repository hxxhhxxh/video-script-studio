# 部署到云服务器（永久网址）

目标：把工作台搬到一台常驻服务器上，电脑关机也能访问。

## 一、买什么机器

| 方案 | 配置 | 价格参考 | 适合度 |
|------|------|----------|--------|
| 国内轻量应用服务器（腾讯云/阿里云，新用户活动） | 2 核 4G / 40G 盘 | 约 ¥99–300/年 | **推荐**。国内节点抓抖音/B站正常 |
| 国内轻量 | 2 核 2G | 约 ¥30–60/月 | 能用，Whisper small 需把并发压到 1 |
| 海外 VPS（Vultr/DigitalOcean 等） | 2 核 4G | $5–6/月 | 便宜，但抓国内平台常被挡，且要配代理 |

最低要求：**2 核 / 2G 内存 / 20G 盘**，Debian 12 或 Ubuntu 22.04。

## 二、上线（三条命令）

在服务器上执行：

```bash
# 1. 装 Docker（国内机器用这个脚本更快）
curl -fsSL https://get.docker.com | sh

# 2. 把项目传上去（在你自己电脑上跑，或者用 git）
scp -r video-script-studio root@<服务器IP>:/opt/

# 3. 在服务器上启动
cd /opt/video-script-studio
echo "VSS_ACCESS_TOKEN=$(head -c 32 /dev/urandom | od -An -tx1 | tr -d ' \n')" > .env
docker compose up -d --build
```

看日志：`docker compose logs -f`

## 三、对外开放

- **有域名**：在域名解析里把 `你的域名` A 记录指向服务器 IP，然后装 Caddy 自动签 HTTPS：
  ```bash
  docker run -d --name caddy --restart unless-stopped \
    -p 80:80 -p 443:443 \
    -v $PWD/Caddyfile:/etc/caddy/Caddyfile \
    -v caddy_data:/data caddy
  ```
  Caddyfile 内容（把域名换成你的）：
  ```
  你的域名 {
      reverse_proxy 127.0.0.1:8790
  }
  ```
- **没域名**：直接 `http://<服务器IP>:8790` 也能用，但没有 HTTPS，口令会以明文传输，**不建议长期这样**。

## 四、首次启动要等

容器第一次启动会下载语音模型（约 460MB，从 hf-mirror 取），之后缓存在 `vscript-data` 卷里，重建容器也不丢。

## 五、建议同时做的两件事

1. **配云端大模型 key**（`docker-compose.yml` 里注释掉的三行）。服务器 CPU 通常比你的笔记本弱，本地 7B/8B 会更慢；走云端 API 一条视频几十秒出结果。
2. **限制入口**：Docker 里跑的应用没有 shell 风险，但下载器能被滥用（拿你服务器当代理抓视频）。口令要设长，必要时在防火墙里限定来源 IP。

## 六、更新代码后

```bash
cd /opt/video-script-studio
git pull        # 或重新 scp
docker compose up -d --build
```
